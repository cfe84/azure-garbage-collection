deployment:
  resources:
  - type: appRegistration
    functionApp: default
  - type: functionApp
    deploy: true
    identityScope: $SUBSCRIPTIONID
    settings:
    - "SUBSCRIPTION=$SUBSCRIPTION_RESOURCE_ID"
    - "DEFAULT_EXPIRY_DAYS=14"
    - "DEFAULT_TAG=experiment"
    appRegistration: default